import React from "react";

const PartnerContractor = () => {
  return (
    <section className="mx-auto w-full max-w-[1224px] px-2 py-5">
      <h3 className="text-mana section-heading text-center">
        Why partner with Contractor+?
      </h3>
      <p className="text-center text-base font-semibold text-[#5C5C5C] md:text-lg lg:text-xl xl:text-2xl">
        Contractors build estimates, generate material lists, and move straight
        to ordering without leaving Contractor+. That means your products show
        up at the exact moment a buyer is specifying quantities and approving
        budgets—no extra portals, no friction. Our job is to reduce clicks
        between “need” and “ordered,” so you win more baskets with less effort.
        <span className="flex pt-5">
          We already power fast quotes with live material pricing and one‑flow
          approvals/eSign, so adding your catalog completes the loop from
          estimate → order → job.
        </span>
      </p>
    </section>
  );
};

export default PartnerContractor;
